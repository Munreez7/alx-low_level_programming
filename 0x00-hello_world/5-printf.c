#include <stdio.h>
int main(void)
	/**
	 * Main - prints the outlined text
	 * Return: Always 0 for success
	 */
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
